using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PathFindingTest : MonoBehaviour
{
    byte[,] layout;
    // Start is called before the first frame update
    void Start()
    {
        layout = new byte[100,100]; 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
